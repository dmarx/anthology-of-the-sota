### Changed

- **The luria pin is `0.21.1`**, from `0.20.4`. Nothing about this record
  changes; the lint just stops doing the same work repeatedly.

  `luria lint` over this record goes **128.2s → 17.1s**, measured here on
  both versions. The three releases in between cache a scheme's directory
  listing, route every document read through one cache, and cache the
  directive scans — all of it work this record was paying for on every CI
  run.

  Verified before pinning rather than after: lint output is byte-identical
  across the two versions, all 87 generated views hash identically, and
  `luria site` stages 633 pages with no links redirected to the repository.
