---
number: 93
status: 'Active'
title: 'Consistency Models'
version: 3
history:
- version: 2
  date: '2026-09-09'
  note: >-
    Read against the paper. Takeaways replaced with the actual
    mechanism, numbers and scope.
- version: 3
  date: '2026-09-10'
  note: >-
    Now the source of two practices filed by the #121 audit: keep a
    multi-step option, and anneal a discretisation. The Standing
    section's 'nothing is sourced here' recorded a pass policy, not a
    finding.
tags:
- generative-modeling
date: '2026-08-24'
published: '2023-03-01'
arxiv: '2303.01469'
first_author: 'Song'
keywords:
- 'diffusion-models'
- 'efficiency'
- 'generative-models'
- 'sampling'
summary: >-
  Song et al. (2023), [ARXIV-2303.01469](https://arxiv.org/abs/2303.01469). Single-step generation.
---

# LIT-093: Consistency Models

Song et al. (2023) — [ARXIV-2303.01469](https://arxiv.org/abs/2303.01469)

## Key takeaways

- Trains a model to map **any** point on a diffusion trajectory directly to its
  origin — self-consistency along the trajectory — so generation is **one**
  function evaluation. Multistep sampling stays available, so the compute/quality
  dial survives
- **Two training modes, and the second is the contribution.** Consistency
  *distillation* needs a pretrained diffusion model; consistency *training*
  needs none, which makes these an independent model family rather than an
  acceleration technique
- **One-step FID 3.55 on CIFAR-10, 6.20 on ImageNet-64**, beating prior
  distillation methods; trained in isolation they beat existing one-step
  **non-adversarial** models on CIFAR-10, ImageNet-64 and LSUN-256
- **A bias/variance schedule on the discretisation:** the training loss has less
  variance and more bias when `N` is small and the reverse when `N` is large, so
  `N` is *increased* during training on a schedule, with the EMA decay `μ`
  scheduled alongside it. Fast biased progress early, accurate noisy refinement
  late
- Zero-shot inpainting, colorization and super-resolution with no task-specific
  training. The authors flag that Theorem 3 is **vacuous for the `ℓ₁` norm** and
  prove a separate statement

## Standing in the anthology

Read — [NOTE-053](../notes.d/NOTE-053.md).

The source of two practices, filed by the [#121](https://github.com/dmarx/anthology-of-the-sota/issues/121) audit: [SOTA-206](../practices.d/SOTA-206.md)
<!-- inactive-ok: SOTA-204 — Proposed, and named here as Proposed: this is the practice filed from R2, and its status is the point being made -->
(keep a multi-step option in a few-step model) and [SOTA-204](../practices.d/SOTA-204.md)
(anneal a discretisation from coarse to fine), the second `Proposed` because
this is the only instance the record holds of a general-looking mechanism.

The transferable finding is the **discretisation schedule**, and the record has
<!-- inactive-ok-block: SOTA-141, SOTA-156 — both Proposed, named as the record's schedule neighbourhood rather than relied on -->
nothing like it. Its schedule practices are about learning rates (`SOTA-140`,
`SOTA-141`, `SOTA-156`) and batch sizes; this schedules a *step count* on an
explicit bias/variance argument, and the same shape applies wherever a loss
approximates its target through a discretisation.

The takeaways this replaced had the emphasis backwards: **"distillation of
diffusion models" is the mode the paper is at pains to say it does not
require.**
