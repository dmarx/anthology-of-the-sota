<!-- GENERATED by `luria index` — do not edit. -->

# ADRs with `status` `Proposed`

*Decision statuses — Whether a decision is in force. Shared by ADR and DP, because as far as being in force goes a principle and a decision are the same kind of object.*

**Proposed** — not in force yet — an open question, so citing it as settled is what the reference report catches.

2 of 59 decisions. Back to the [full index](../README.md).

| # | Title | Summary | Status |
|---|---|---|---|
| [ADR-045](../../../record/decisions.d/ADR-045.md) v3 | A claim about the signal is admissible however it was measured, including on people | A psycholinguistics paper arrived with a real measurement, no model and no recommendation, and the first instinct was to file it as a LIT note that sources nothing — admitted on an exception. That was wrong twice over: the record already holds physics, control theory and pure mathematics, so "outside CS" was never the boundary; and the paper's finding is a claim about what is TRUE, which is what THEORY holds. Proposed: the scheme follows the kind of claim, not the apparatus that produced it, so a finding about the signal is a THEORY whether it was measured on a GPU or on 755 people — provided its document says what it does not license. Rejected: the exception framing, and sourcing a practice from it. Adding a topic, which v1 rejected, is taken in v3 under [ADR-059](../../../record/decisions.d/ADR-059.md): `signal-structure`. | Proposed |
| [ADR-058](../../../record/decisions.d/ADR-058.md) | A library is evidence about consensus, and its observation lives in a report | A repository that implements a technique is evidence for `consensus` and for nothing else. The judgement lands in `consensus_note`, naming the libraries checked and the date; the observation lands in a generated report, pinned by ref and recomputable. A library does not become a `LIT` note — its paper may already be one. Rejected: `implementations:`, which already means four things and would be made permanently unreadable by a fifth. | Proposed |
