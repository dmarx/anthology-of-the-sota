---
number: 81
status: Active
title: 'GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers'
version: 3
history:
- version: 1
  date: '2026-08-24'
  note: >-
    Filed by the migration against the correct identifier with four bullets
    — model quantization strategy, minimal accuracy impact, memory
    reduction, efficient inference — which describe the category and not the
    method.
- version: 2
  date: '2026-09-08'
  note: >-
    Takeaways written against the paper. Sourced a practice, which it had
    never done in a year in the corpus.
- version: 3
  date: '2026-09-10'
  note: >-
    The paragraph explaining why this note went unfiled described a tag
    vocabulary that no longer exists. ADR-021 added the topic in
    question to the practice side and ADR-tmpdrbma made the two
    vocabularies identical, so no LIT tag can read as a scope boundary.
    The observation about unfiled notes stands.
tags:
- inference-optimization
date: '2026-08-24'
published: '2022-10-31'
arxiv: '2210.17323'
first_author: 'Frantar'
keywords:
- 'quantization'
- 'post-training'
- 'error-compensation'
- 'memory-bandwidth'
summary: >-
  Frantar et al. (2022), [ARXIV-2210.17323](https://arxiv.org/abs/2210.17323). One-shot weight quantization to
  3-4 bits: round column by column and push each rounding error into the
  columns not yet done, using approximate second-order information from a
  small calibration set.
---

# LIT-081: GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers

Frantar et al. (2022) — [ARXIV-2210.17323](https://arxiv.org/abs/2210.17323)

## Key takeaways

- The problem with round-to-nearest is that it treats weights as
  independent. They are not: the layer's output error depends on how the
  weights interact, so a rounding error made in one column can be absorbed
  by adjusting columns that have not been quantized yet.
- The method quantizes column by column and compensates, using approximate
  second-order (Hessian) information estimated from a small calibration set.
  This is a one-shot procedure — no retraining, no gradient steps.
- The headline: 3- and 4-bit weight quantization of models up to 175B in
  roughly four GPU-hours, with accuracy close to the uncompressed baseline.
  Reaching 4 bits at that scale had previously meant quantization-aware
  training.
- It is weight-only. The win is memory footprint and bandwidth, which is
  what makes decode faster, and it does nothing for a compute-bound prefill.
- The calibration set is an input, not an implementation detail. A
  calibration distribution unlike the serving distribution is where this
  degrades.

## Standing in the anthology

The source of [SOTA-185](../practices.d/SOTA-185.md), which files the approach rather than the
artifact: error-compensating one-shot PTQ with a calibration set is what
converged, and the specific search and ordering have been improved on
repeatedly since.

Unfiled for a year, and the diagnosis has since been overtaken twice. The note
is tagged `inference-optimization`, which **was** not one of the seven topics a
practice could carry — but the record already held quantization practices under
`systems-optimization` and `training-optimization`, so the tag was concealing
the paper rather than excluding it.

`ADR-021` added `inference-optimization` to the practice vocabulary, and
`ADR-tmpdrbma` has since made the two vocabularies identical. **No `LIT` tag can
look like a scope boundary any more**, because every one of them is a word a
practice can carry. That removes the mechanism this paragraph describes; the
thing it was a symptom of — a note nobody filed a practice from and nobody
recorded a reason for — is the part still worth watching.
