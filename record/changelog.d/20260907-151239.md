<!-- No user-facing change: `remotes.lock.json` gains the three arXiv
     identifiers #49 introduced, so the source-mismatch check has something
     to compare their titles against. The lockfile records what upstream
     serves; it is not a claim of this record's own. -->
