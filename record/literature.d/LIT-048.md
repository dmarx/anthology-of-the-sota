---
number: 48
status: 'Active'
title: 'Train Short, Test Long: Attention with Linear Biases Enables Input Length Extrapolation'
version: 4
history:
- version: 2
  date: '2026-09-09'
  note: >-
    Read against the paper. Takeaways replaced with the actual
    mechanism, numbers and scope.
- version: 3
  date: '2026-09-10'
  note: >-
    The
- version: 4
  date: '2026-09-24'
  note: >-
    Gains the measurement that says what ALiBi bought and what it cost.
    LIT-638 scores attention resolution across the extrapolation
    boundary: ALiBi 0.81 to 0.88, the only incumbent that holds, against
    RoPE's 0.91 to 0.08 -- and prices it at 0.2-0.3 perplexity. "The
    alternative that did not win" is right about adoption and was incomplete
    about the axis ALiBi was built for.
tags:
# Retagged from the report of unbound lineage. This is a positional-encoding
# paper, and `representation-and-encoding` names positional encoding in its
# own blurb -- the topic #114 added after this note was filed, which is why
# it was carrying the nearest thing available at the time.
- representation-and-encoding
# Secondary, restored: ALiBi is an attention bias, so it is architecture as much as encoding (ADR-035).
- model-architecture
date: '2026-08-24'
published: '2021-08-01'
arxiv: '2108.12409'
first_author: 'Press'
keywords:
- 'transformers'
- 'attention'
- 'efficiency'
- 'extrapolation'
summary: >-
  Press et al. (2021), [ARXIV-2108.12409](https://arxiv.org/abs/2108.12409). Linear attention bias.
compared_against:
- LIT-207
- LIT-638
---
<!-- inactive-ok-file: SOTA-179 — Proposed; named as one of the two ways this record already recovers extrapolation after the field preferred RoPE's perplexity to ALiBi's native extrapolation, not relied on as a recommendation -->

# LIT-048: Train Short, Test Long: Attention with Linear Biases Enables Input Length Extrapolation

Press et al. (2021) — [ARXIV-2108.12409](https://arxiv.org/abs/2108.12409)

## Key takeaways

- Deletes position embeddings entirely and adds a **fixed, non-learned linear
  penalty** to each attention score, proportional to query–key distance:
  `softmax(q_i·k_j + m·−(i−j))`, with one slope `m` per head
- Slopes are the geometric sequence starting at `2^(−8/n)` — for 8 heads
  `1/2¹ … 1/2⁸` — found by **"a brief manual exploration of around ten slope
  sets"**. The stated rule of thumb: slopes in (0,1), density increasing
  toward 0. **Making the slopes trainable did not extrapolate well and cost 3%
  training speed**
- Sinusoidal embeddings **cannot extrapolate at all** — degrading as soon as
  one token past `L` is added, at both `L = 512` and `L = 1024`. The **T5 bias
  also extrapolates**; Raffel et al. proposed that and never tested it, and
  this paper is where it is tested
- At 1.3B on a 461 GB corpus, ALiBi matches sinusoidal perplexity while
  **running faster and using less memory** — because it can be trained on
  shorter subsequences. The practical argument is training cost, not
  long-context quality
- **The authors' own deflation, Appendix B:** ALiBi's edge is *"largely
  explained by its improved avoidance of the early token curse"*, and future
  work "might achieve further gains by more efficiently exploiting longer
  histories" — i.e. ALiBi is not doing that

## Standing in the anthology

Read — [NOTE-028](../notes.d/NOTE-028.md). Nothing is sourced to this paper; RoPE won, and ALiBi is
carried as the alternative that did not.

**That is right about adoption and was incomplete about the measurement.** On
the one axis ALiBi was built for, it is the only incumbent that works.
[LIT-638](LIT-638.md) scores attention resolution — how well an attention pattern
distinguishes token distance — at a 1024 training length and at 2048:

| variant | 1024 | 2048 |
| --- | --: | --: |
| RoPE | **0.91** | **0.08** |
| absolute sinusoidal | 0.87 | 0.28 |
| **ALiBi** | 0.81 | **0.88** |

RoPE is the best of the three in-distribution and collapses by an order of
magnitude past the boundary. ALiBi is the weakest inside and **the only one
that survives**.

And the trade has a price: ALiBi's perplexity runs **0.2–0.3 above RoPE's**,
which the same paper attributes to the mechanism — the exponential decay acts
"like a soft sliding window", and "the absence of long-term dependency
contributes to a performance drop". Its stability "comes from explicit decay,
but it prevents the model from learning position dependency itself."

So the reason nothing is sourced here is not that ALiBi failed at its own
task. It is that the field preferred 0.2–0.3 perplexity to native
extrapolation, and then recovered the extrapolation by other means —
`SOTA-151` rescales RoPE, `SOTA-179` truncates its low frequencies. **A
paper can lose on adoption while being right about the thing it measured**,
and this note previously recorded only the first half.

**The previous takeaway "theoretical analysis" was false.** There is no
theoretical analysis in this paper: the slopes come from trying about ten sets
by hand, and the mechanism claim is empirical. That bullet attributed to the
paper the one kind of contribution it does not make.

The reading's real value is the **early token curse**, and it belongs to the
record rather than to ALiBi. Every long-context claim in the corpus is a claim
that a model does better with more context — and here the authors of the
technique say most of their own measured improvement was *not* that, but the
easier job of predicting fewer tokens that have little context behind them.
The record states that confound nowhere.

**Audited by [#121](https://github.com/dmarx/anthology-of-the-sota/issues/121); unsourced.** R1 is the recommendation worth
having and it is an evaluation caution, not an architecture one: **separate the
early-token effect from genuine long-range use before claiming the latter.** It
is the authors' own caveat about their own headline result, which is what makes
it credible.
<!-- inactive-ok: ADR-027 — Superseded for its blurb only; the topic it added is the one this note is filed against -->
It belongs with the `analysis-and-evaluation` practices [ADR-027](../decisions.d/ADR-027.md)
made fileable and is the strongest single candidate left in that group.
